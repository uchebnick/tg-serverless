apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: telegram-serverless
  labels:
    app: kafka
    component: messaging
spec:
  type: ClusterIP
  selector:
    app: kafka
  ports:
  - name: kafka
    port: 9092
    targetPort: 9092
    protocol: TCP
